Catabi's Nixos Dotfiles

```git clone https://github.com/catabi/nixos-dotfiles.git```

When i meow in a commit message it's usually just me being lazy and it being the same reason as the last commit :3
